[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "torch-tracker"
version = "0.1.0"
description = "Incremental yt-based analysis tracker for Torch"
dependencies = ["yt", "h5py", "numpy"]
